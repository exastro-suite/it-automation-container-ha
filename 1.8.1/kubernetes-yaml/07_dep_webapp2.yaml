apiVersion: apps/v1
kind: Deployment
metadata:
  name: webapp2
  namespace: itaha01
  labels:
    app: webapp2
spec:
#  replicas: 1
  replicas: 2
  selector:
    matchLabels:
      app: webapp2
  template:
    metadata:
      labels:
        app: webapp2
    spec:
      hostAliases:
      - ip: "127.0.0.1"
        hostnames:
        - "exastro-it-automation"
      containers:
      - name: webapp2
        image: it-automation-webapp:1.8.1-ja-centos8
        envFrom:
        - configMapRef:
            name: cm-data2
        - prefix:
          secretRef:
            name: secret-data
        ports:
        - containerPort: 80
        securityContext:
          privileged: true
        volumeMounts:
          - name: exastro-file-pv2
            mountPath: /exastro-file-volume
      volumes:
      - name: exastro-file-pv2
        persistentVolumeClaim:
          claimName: exastro-file-pvc2
      terminationGracePeriodSeconds: 10
