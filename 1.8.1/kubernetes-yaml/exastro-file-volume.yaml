apiVersion: v1
kind: PersistentVolume
metadata:
  name: exastro-file-volume
spec:
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteMany
  nfs:
    server: 10.197.17.186
    path: /exastro-file

---

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: exastro-file-volume
  labels:
    app: exastro-file-volume
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
